import type { FormAnswers } from '../types/form';
import { evaluateTrafficCase, getApplicableTrafficRules } from './legalRules';
import { runTransitLegalQualityGate } from './transitLegalQualityGate';
import { buildTrafficPrescriptionDoctrine } from './trafficLegalDoctrine';

const v=(a:FormAnswers,k:string,f='')=>{const x=a[k];if(Array.isArray(x))return x.join(', ');if(typeof x==='boolean')return x?'Sí':'No';return x==null?f:String(x)};
const fmt=(value?:string)=>value?new Date(`${value}T00:00:00`).toLocaleDateString('es-CO'):'';
type TrafficComparendo={number:string;violationDate?:string;coactiveDate?:string;origin?:string;infraction?:string;totalFine?:number;paymentOrderNoticeDate?:string};

export function buildTrafficDocument(slug:string,a:FormAnswers){
 const title: string = {'prescripcion-comparendo':'SOLICITUD DE PRESCRIPCIÓN DE OBLIGACIÓN DE TRÁNSITO','caducidad-comparendo':'SOLICITUD DE REVISIÓN DE CADUCIDAD DE ACTUACIÓN DE TRÁNSITO','revocatoria-comparendo':'SOLICITUD DE REVOCATORIA / CORRECCIÓN DE ACTUACIÓN DE TRÁNSITO','solicitud-soportes-comparendo':'DERECHO DE PETICIÓN — SOLICITUD DE INFORMACIÓN Y SOPORTES DE TRÁNSITO','fotomultas':'DERECHO DE PETICIÓN — SOLICITUD RELACIONADA CON FOTODETECCIÓN / FOTOMULTA'}[slug]??'SOLICITUD ADMINISTRATIVA DE TRÁNSITO';
 const structured=Array.isArray(a.comparendos);
 if(slug==='prescripcion-comparendo' && structured){
   const comparendos:TrafficComparendo[]=Array.isArray(a.comparendos)?(a.comparendos as unknown as TrafficComparendo[]):[];
   const result=runTransitLegalQualityGate({applicant:{fullName:v(a,'fullName'),documentType:v(a,'documentType','CC'),documentNumber:v(a,'documentNumber'),email:v(a,'email')},authority:{name:v(a,'authorityName'),municipality:v(a,'authorityMunicipality'),department:v(a,'authorityDepartment')},comparendos});
   const rows=comparendos.map((c,i)=>`${i+1}. Comparendo ${c.number || 'sin número'}${c.infraction?` — infracción ${c.infraction}`:''}${c.violationDate?` — fecha de infracción ${fmt(c.violationDate)}`:''}${c.coactiveDate?` — fecha de cobro coactivo ${fmt(c.coactiveDate)}`:''}${c.paymentOrderNoticeDate?` — notificación del mandamiento ${fmt(c.paymentOrderNoticeDate)}`:''}${c.origin?` — origen ${c.origin}`:''}${c.totalFine!==undefined?` — valor reportado $${Number(c.totalFine).toLocaleString('es-CO')}`:''}`).join('\n');
   const analyses=result.analyses.map((analysis:any,i:number)=>`• Comparendo ${i+1}: ${analysis.summary || analysis.reason || 'Revisión del término y del expediente requerida.'}`).join('\n');
   const warning=result.issues.filter((i:any)=>i.severity==='warning').map((i:any)=>`• ${i.message}`).join('\n');
   const doctrine=comparendos.map((c,i)=>buildTrafficPrescriptionDoctrine({qualityLevel:result.level,analysis:result.analyses[i],comparendoNumber:c.number || String(i+1)}).join('\n\n')).join('\n\n');
   const petitioner=`${v(a,'fullName')}, identificado(a) con ${v(a,'documentType','CC')} No. ${v(a,'documentNumber')}`;
   return [v(a,'authorityMunicipality','Ciudad'),new Date().toLocaleDateString('es-CO'),' ',v(a,'authorityName',`AUTORIDAD DE TRÁNSITO COMPETENTE DE ${v(a,'authorityMunicipality','')}`),v(a,'authorityMunicipality',''),v(a,'authorityDepartment',''),' ',title,' ',petitioner,' ','I. OBJETO','Por medio del presente escrito solicito la revisión integral de las obligaciones de tránsito relacionadas y, si se encuentran acreditados los presupuestos legales, la declaratoria de prescripción de la acción de cobro respecto de cada obligación. De manera subsidiaria, solicito que se examine la pérdida de ejecutoriedad cuando concurran los presupuestos del artículo 91 numeral 3 del CPACA.',' ','II. INFORMACIÓN DE LAS OBLIGACIONES',rows,' ','III. HECHOS Y ANTECEDENTES','El solicitante relaciona las obligaciones anteriores y solicita que la autoridad confronte la información suministrada con los expedientes administrativos correspondientes. La solicitud se formula a partir del transcurso del tiempo y de la necesidad de verificar de manera individual las actuaciones de cobro, sus fechas y sus constancias de notificación.',' ','IV. FUNDAMENTO JURÍDICO',doctrine,' ','V. ANÁLISIS PRELIMINAR DE TRÁMITE',`TrámiteYa realizó una verificación preliminar con base exclusivamente en los datos disponibles. El nivel de preparación del trámite es ${result.score}%, clasificado como ${result.level === 'green' ? 'verde' : result.level === 'yellow' ? 'amarillo' : 'rojo'}. Este indicador no representa una probabilidad de éxito ni sustituye el examen del expediente administrativo.`,analyses||'• Se requiere revisión individual del expediente de cada obligación.',warning?`\nAdvertencias de verificación:\n${warning}`:'',' ','VI. PETICIONES','PRIMERO. Que se verifique, respecto de cada obligación relacionada, la fecha de ocurrencia del hecho, la fecha de ejecutoria del acto sancionatorio, la existencia y fecha de expedición del mandamiento de pago, su fecha y forma de notificación y las actuaciones posteriores que puedan afectar el cómputo del término.','SEGUNDO. Que, si del expediente se acredita la configuración de la prescripción de la acción de cobro, se declare de oficio y se adopten las consecuencias administrativas que jurídicamente correspondan.','TERCERO. De manera subsidiaria, que se examine la pérdida de ejecutoriedad del acto administrativo conforme al artículo 91 numeral 3 de la Ley 1437 de 2011, si se acredita que transcurrieron cinco (5) años desde su firmeza sin que la autoridad hubiera realizado los actos que le correspondían para ejecutarlo.','CUARTO. Que se actualicen o depuren los registros administrativos y sistemas de información que correspondan, conforme a la decisión adoptada y a las reglas aplicables.','QUINTO. Que, si alguna obligación se considera vigente, se remita copia íntegra y legible del expediente, incluyendo título ejecutivo, acto sancionatorio, constancia de ejecutoria, mandamiento de pago, constancia de notificación, actuaciones posteriores, acuerdos o facilidades de pago y medidas cautelares, si existen.','SEXTO. Que la respuesta sea de fondo, clara, congruente, completa y diferenciada respecto de cada obligación individualmente considerada.',' ','VII. NOTIFICACIONES',`El solicitante recibirá respuesta en el correo electrónico ${v(a,'email')}.`,' ','Atentamente','',v(a,'fullName'),`${v(a,'documentType','CC')} No. ${v(a,'documentNumber')}`,`Correo: ${v(a,'email')}`].join('\n');
 }
 const rules=getApplicableTrafficRules(a); const decisions=evaluateTrafficCase(a); const favorable=decisions.filter(d=>d.level==='favorable'); const uncertain=decisions.filter(d=>d.level!=='favorable');
 const ruleLabels=rules.filter(r=>r.id!=='soportes').map(r=>r.label).join(', ');
 const primaryRequest=favorable.length?`Solicito que se declare o reconozca la procedencia de ${favorable.map(d=>d.id==='prescripcion'?'la prescripción':d.id==='caducidad'?'la caducidad':d.label.toLowerCase()).join(' y ')}, previo el análisis integral del expediente.`:`Solicito que se verifique la procedencia de ${ruleLabels||'la actuación solicitada'} y se adopte la decisión jurídicamente correspondiente, sin presumir la existencia de los presupuestos que deban acreditarse.`;
 const evidenceRequest=uncertain.length?`Para resolver lo anterior, solicito especialmente: ${uncertain.map(d=>d.nextStep).join(' ')}.`:'Solicito copia íntegra del expediente y de los soportes pertinentes.';
 const soportes=`Solicito copia íntegra de los soportes que sustentan la actuación, incluyendo comparendo, evidencia, constancias de notificación, mandamiento de pago si existe, actos administrativos, recursos y demás documentos pertinentes.`;
 const legalAnalysis=decisions.length?decisions.map(d=>`• ${d.label}: ${d.reason} Siguiente actuación: ${d.nextStep} Fundamento orientador: ${d.legalBasis.join('; ')}.`).join('\n'):'No se identificó una decisión jurídica automatizada concluyente. Se requiere revisión del expediente y de la información aportada.';
 return [v(a,'ciudad','Ciudad'),v(a,'fecha',new Date().toLocaleDateString('es-CO')),'',v(a,'entidad',v(a,'autoridad','SEÑOR(A) AUTORIDAD DE TRÁNSITO')),'',title,'',`Solicitante: ${v(a,'nombres')} ${v(a,'apellidos')}`,`Documento: ${v(a,'documento')}`,`Correo: ${v(a,'correo')}`,`Comparendo / acto: ${v(a,'numero_comparendo')}`,`Fecha: ${v(a,'fecha_comparendo')}`,`Placa: ${v(a,'placa')}`,'','I. OBJETO',primaryRequest,'','II. HECHOS',v(a,'hechos'),'','III. ANÁLISIS JURÍDICO PRELIMINAR',legalAnalysis,'','IV. PETICIONES',primaryRequest,`Solicito además que ${evidenceRequest.toLowerCase()}`,'Se remita respuesta de fondo, clara, congruente y completa dentro del término legal aplicable.','','V. INFORMACIÓN Y SOPORTES',soportes,'','VI. ANEXOS',v(a,'anexos','No se relacionan anexos.'),'','Atentamente','',`${v(a,'nombres')} ${v(a,'apellidos')}`,`C.C. ${v(a,'documento')}`].join('\n')}
