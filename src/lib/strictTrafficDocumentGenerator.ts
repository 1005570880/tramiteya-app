import type { FormAnswers } from '../types/form';
import type { Procedure } from '../types';
import type { DocumentItem } from '../types/procedure';
import { assessLegalSituation, generateUnifiedLegalDocument, sanitizeValue, type SelectedRecordData } from './legalEngine';
import { evaluateTrafficCase } from './legalRules';
const TRAFFIC_SLUGS = new Set(['prescripcion-comparendo','caducidad-comparendo','revocatoria-comparendo','solicitud-soportes-comparendo','fotomultas','derecho-de-peticion-eliminar-multa']);
function objectValue(value: unknown): Record<string, any> { return value && typeof value === 'object' && !Array.isArray(value) ? value as Record<string, any> : {}; }
function pick(...values: unknown[]): string | undefined { const value = values.find((candidate) => candidate !== undefined && candidate !== null && sanitizeValue(candidate) !== ''); return value === undefined || value === null ? undefined : sanitizeValue(value); }
function normalizeAuthority(value: unknown, municipality: unknown): string | undefined { const authority = sanitizeValue(value); const city = sanitizeValue(municipality); if (!authority) return city ? `SECRETARÍA DE TRÁNSITO Y MOVILIDAD DE ${city.toUpperCase()}` : undefined; const upper = authority.toUpperCase(); if (/SECRETAR[IÍ]A|INSPECCI[ÓO]N|ORGANISMO DE TR[AÁ]NSITO|TR[AÁ]NSITO Y MOVILIDAD|TR[AÁ]NSITO Y TRANSPORTE/.test(upper)) return authority; return city ? `SECRETARÍA DE TRÁNSITO Y MOVILIDAD DE ${city.toUpperCase()}` : authority; }
function toRecord(answers: FormAnswers): SelectedRecordData { const a = answers as FormAnswers & Record<string, any>; const simit = objectValue(a.__simitRecord); const questionnaire = objectValue(a.__tramiQuestionnaire || a.tramiAnswers); const municipality = pick(a.ciudad, a.municipio, simit.municipality); return { comparendo: pick(a.numero_comparendo, simit.number), fecha: pick(a.fecha_comparendo, simit.date), organismo: normalizeAuthority(pick(a.entidad, a.autoridad, simit.authority), municipality), estado: pick(a.estadoComparendo, a.estado, simit.status), valor: pick(a.valor, a.valor_multa, simit.value), placa: pick(a.placa, simit.plate), cedula: pick(a.documento, a.documentNumber, a.cedula, simit.documentNumber), codigo: pick(a.codigo_infraccion, a.codigoInfraccion, simit.infractionCode, simit.code), nombre: pick(a.nombre, a.nombreCompleto, simit.ownerName, simit.name), correo: pick(a.correo, a.email, simit.email), telefono: pick(a.telefono, a.phone, simit.phone), fechaResolucion: pick(a.fechaResolucion, a.fecha_resolucion, simit.resolutionDate), fechaNotificacion: pick(a.fechaNotificacion, a.fecha_notificacion, simit.notificationDate), fechaMandamientoPago: pick(a.fechaMandamientoPago, a.fecha_mandamiento_pago, simit.mandamientoDate, simit.paymentOrderDate), fechaNotificacionMandamiento: pick(a.fechaNotificacionMandamiento, a.fecha_notificacion_mandamiento, simit.paymentOrderNotificationDate), fechaEjecutoria: pick(a.fechaEjecutoria, a.fecha_ejecutoria, simit.executedDate), huboAudiencia: a.huboAudiencia ?? a.hubo_audiencia ?? questionnaire.audiencia, existeResolucion: a.existeResolucion ?? a.existe_resolucion ?? questionnaire.resolucion, actuacionesCobro: pick(a.actuacionesCobro, questionnaire.cobro, simit.collectionActions), esFotodetencion: Boolean(a.esFotodetencion || /foto|fotomult|fotodetecci[oó]n|c[aá]mara/i.test(String(questionnaire.causal || questionnaire.causal_principal || a.causal || '')) || String(a.codigoInfraccion || simit.infractionCode || '').toUpperCase() === 'C35'), tramiAnswers: questionnaire, tramiConocimiento: sanitizeValue(questionnaire.conocimiento), tramiNotificacion: sanitizeValue(questionnaire.notificacion), tramiAudiencia: sanitizeValue(questionnaire.audiencia), tramiResolucion: sanitizeValue(questionnaire.resolucion), tramiCobro: sanitizeValue(questionnaire.cobro), tramiPagos: sanitizeValue(questionnaire.pagos), tramiEvidencia: sanitizeValue(questionnaire.evidencia) }; }
function cleanCitizenLanguage(content: string): string { return content.replace(/La v[ií]a principal identificada por Tr[aá]mi es\s*/gi, '').replace(/Tr[aá]mi no presenta esa fecha como prescripci[oó]n configurada:?/gi, 'La configuración de la prescripción depende de la cronología y de las actuaciones jurídicamente eficaces acreditadas.').replace(/Tr[aá]mi(?:\s+determinar[aá]|\s+identific[aó]|\s+eval[uú]a|\s+consider[aó])[^.]*\.?/gi, '').replace(/\bTr[aá]mi\b/gi, '').replace(/triaje(?:\s+conversacional)?/gi, '').replace(/\bno_recuerdo\b/gi, 'no fui notificado formalmente por la autoridad').replace(/\bsolo_simit\b/gi, 'el único documento disponible al momento de esta petición es el Estado de Cuenta del SIMIT').replace(/El solicitante identificado para el tr[aá]mite es ([^.]+)\./gi, 'Me identifico como $1.').replace(/El solicitante manifiesta que no recibió/gi, 'Manifiesto que no recibí').replace(/El solicitante indica que conoci[oó] por primera vez la actuaci[oó]n:/gi, 'Indico que conocí por primera vez la actuación:').replace(/El solicitante reporta una actuaci[oó]n de cobro/gi, 'Manifiesto que he identificado una actuación de cobro').replace(/El solicitante manifiesta:/gi, 'Manifiesto:').replace(/El solicitante se[nñ]ala:/gi, 'Señalo:').replace(/El solicitante indica:/gi, 'Indico:').replace(/El solicitante reporta:/gi, 'Manifiesto:').replace(/El solicitante/gi, 'Yo').replace(/\n{3,}/g, '\n\n').trim(); }
const ORDINALS = ['PRIMERO','SEGUNDO','TERCERO','CUARTO','QUINTO','SEXTO','SÉPTIMO','OCTAVO','NOVENO','DÉCIMO','UNDÉCIMO','DUODÉCIMO','DECIMOTERCERO','DECIMOCUARTO','DECIMOQUINTO','DECIMOSEXTO','DECIMOSÉPTIMO','DECIMOCTAVO','DECIMONOVENO','VIGÉSIMO'];
function isListLine(line: string): boolean { return /^\s*(?:\d{1,2}[.)]|[A-ZÁÉÍÓÚÑ][.)])\s+/.test(line); }
function ordinalizeSections(content: string): string { const lines = content.split('\n'); let section: 'facts'|'requests'|null = null; let index = 0; return lines.map((line) => { const normalized = line.trim().toUpperCase().replace(/\s+/g,' '); if (/^(?:I{0,3}|IV|V|VI|VII|VIII|IX|X|XI|XII|XIII)\.\s*(?:HECHOS|PRETENSIONES|PETICIONES)\b/.test(normalized) || /^(?:HECHOS|PRETENSIONES|PETICIONES)\b/.test(normalized)) { section = /HECHOS/.test(normalized) ? 'facts' : 'requests'; index = 0; return line; } if (/^[IVXLCDM]+\.\s+/.test(normalized) && !/^(?:HECHOS|PRETENSIONES|PETICIONES)\b/.test(normalized)) section = null; if (section && isListLine(line)) { const body = line.trim().replace(/^\s*(?:\d{1,2}[.)]|[A-ZÁÉÍÓÚÑ][.)])\s+/, ''); const ordinal = ORDINALS[index] || `NÚMERO ${index + 1}`; index += 1; return `${ordinal}: ${body}`; } return line; }).join('\n'); }
function enforceFirstPerson(content: string): string { return content.replace(/\bEl solicitante\s+identificado\s+para\s+el\s+trámite\s+es\s+/gi,'Me identifico como ').replace(/\bEl solicitante\s+manifiesta\s+que\s+no\s+recibi[oó]\s+/gi,'Manifiesto que no recibí ').replace(/\bEl solicitante\s+manifiesta\s*:/gi,'Manifiesto:').replace(/\bEl solicitante\s+se[nñ]ala\s*:/gi,'Señalo:').replace(/\bEl solicitante\s+indica\s*:/gi,'Indico:').replace(/\bEl solicitante\s+indica\s+que\s+/gi,'Indico que ').replace(/\bEl solicitante\s+reporta\s*:/gi,'Manifiesto:').replace(/\bEl solicitante\s+reporta\s+/gi,'Manifiesto que ').replace(/\bEl solicitante\s+informa\s+/gi,'Informo que ').replace(/\bLa persona solicitante\b/gi,'Yo').replace(/\bEl peticionario\b/gi,'Yo').replace(/\bLa parte solicitante\b/gi,'Yo').replace(/\bSe reporta\b/gi,'Manifiesto que se reporta').replace(/\bEl registro identifica\b/gi,'Identifico en el registro').replace(/\bLa actuación aparece asociada\b/gi,'La actuación aparece asociada a mi documento').replace(/\bEl valor reportado\b/gi,'El valor que figura reportado').replace(/\bdebe verificarse\b/gi,'solicito que se verifique').replace(/\bdeben verificarse\b/gi,'solicito que se verifiquen').replace(/\bdebe establecerse documentalmente\b/gi,'solicito que se establezca documentalmente').replace(/\bdebe confrontarse\b/gi,'solicito que se confronte').replace(/\bdeben ser verificadas\b/gi,'solicito que sean verificadas').replace(/\bEl solicitante\b/gi,'Yo'); }
function explicitAuthorityHeader(content: string, municipality: string | undefined, authority: string | undefined): string { const city = sanitizeValue(municipality).toUpperCase(); const rawAuthority = sanitizeValue(authority); const target = city === 'SANTA MARTA' ? 'SECRETARÍA DE TRÁNSITO Y MOVILIDAD DEL DISTRITO DE SANTA MARTA' : rawAuthority && /SECRETAR[IÍ]A|INSPECCI[ÓO]N|ORGANISMO DE TR[AÁ]NSITO|TR[AÁ]NSITO Y MOVILIDAD|TR[AÁ]NSITO Y TRANSPORTE/i.test(rawAuthority) ? rawAuthority.toUpperCase() : city ? `SECRETARÍA DE TRÁNSITO Y TRANSPORTE MUNICIPAL DE ${city}` : rawAuthority.toUpperCase(); const lines = content.split('\n'); let replaced = false; const out = lines.map((line) => { const t = line.trim(); if (!replaced && t && (city && new RegExp(`^${city.replace(/[.*+?^${}()|[\\]\\]/g,'\\$&')}(?:\\s*,.*)?$`,'i').test(t) || (rawAuthority && t.toUpperCase() === rawAuthority.toUpperCase()) || /^(SECRETAR[IÍ]A|INSPECCI[ÓO]N|ORGANISMO DE TR[AÁ]NSITO)/i.test(t))) { replaced = true; return `${target}\nE. S. D.`; } return line; }); if (!replaced && target) { const insertAt = out.findIndex((line) => /^\s*(?:\d{1,2}[\/-])?\d{1,2}[\/-]\d{4}\s*$/.test(line.trim())); if (insertAt >= 0) out.splice(insertAt + 1, 0, target, 'E. S. D.'); else out.unshift(target, 'E. S. D.'); } return out.join('\n').replace(/\n{3,}/g,'\n\n').trim(); }
function applyStrictLegalStyle(content: string, record: SelectedRecordData): string { let styled = cleanCitizenLanguage(content); styled = explicitAuthorityHeader(styled, record.organismo?.replace(/^.*?\b(?:DE|DEL)\s+/i,'') || undefined, record.organismo); styled = ordinalizeSections(styled); styled = enforceFirstPerson(styled); return styled.replace(/\n{3,}/g,'\n\n').trim(); }
function enrich(answers: FormAnswers): FormAnswers { const normalized = { ...(answers as any) } as FormAnswers & Record<string, any>; const record = toRecord(normalized); const assessment = assessLegalSituation(record); const decisions = evaluateTrafficCase(normalized); normalized.__legalAssessment = assessment; normalized.__legalDecisionEngine = { version: 5, generatedAt: new Date().toISOString(), primaryRoute: assessment.primaryRoute, routes: assessment.routes, certainty: assessment.certainty, decisions, evidenceQuestions: assessment.evidenceQuestions, missingEvidence: assessment.missingEvidence, temporal: assessment.temporal }; return normalized; }
export async function generateStrictTrafficDocument(procedure: Procedure, answers: FormAnswers, instanceId?: string): Promise<DocumentItem> { if (!TRAFFIC_SLUGS.has(procedure.slug)) throw new Error(`STRICT_TRAFFIC_GENERATOR_UNSUPPORTED_SLUG: ${procedure.slug}`); const enriched = enrich(answers); const record = toRecord(enriched); const draft = generateUnifiedLegalDocument(record); const content = applyStrictLegalStyle(draft.document?.trim() || '', record); if (!content || content.length < 500) throw new Error('STRICT_LEGAL_ENGINE_EMPTY_DOCUMENT: la biblioteca jurídica no produjo un documento completo.'); const generatedAt = new Date().toISOString(); return { id: `doc_${Date.now()}_${Math.floor(Math.random() * 10000)}`, title: `${procedure.title} - Documento generado`, procedureId: procedure.id, content, createdAt: generatedAt, generatedAt, version: 1, status: 'ready', instanceId, sourceVersion: 'legal-engine-v6-style', snapshot: { answers: JSON.parse(JSON.stringify(enriched)), procedureSlug: procedure.slug, generatedAt, content } }; }
